pom文件
yml文件
主类
业务类

热部署：
 <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
 开启自动build
 setting -》 compiler 全都勾上

 快捷键 三个键(cas) + /
 选中compiler.automake.when.app.runing
 actionSystem.assertFocusAccessFromEdt